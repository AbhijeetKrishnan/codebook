#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int num_tests;
    cin >> num_tests;
    for (int test = 0; test < num_tests; test++) {

    }
    return 0;
}